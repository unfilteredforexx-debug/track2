name: African Stock Market Tracker

on:
  schedule:
    # Every 15 minutes during market hours (adjust timezone as needed)
    - cron: '*/15 3-13 * * *'  # Daily, 3am-1pm UTC
  workflow_dispatch: # Allows manual trigger

jobs:
  run-stock-tracker:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests
      
      - name: Run stock tracker script
        env:
          RAPIDAPI_KEY: ${{ secrets.RAPIDAPI_KEY }}
        run: python stock_tracker.py
      
      - name: Upload results as artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: stock-data-${{ github.run_number }}
          path: |
            *.json
            *.txt
            *.log
          retention-days: 7
